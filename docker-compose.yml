version: '3.8'

services:
    redis:
        container_name: redis-stack
        image: redis/redis-stack:latest
        restart: always
#        env_file:
#          - environments/redis.env
        ports:
            - '6379:6379'
            - '8001:8001'

volumes:
  redis_data:
